# =============================================================================
# Render Blueprint — Infrastructure as Code
# =============================================================================
# После деплоя:
#   1. Добавить 3 env vars в Dashboard →
#      steal-n-scroll-bot → Environment → Add Variable:
#        BOT_TOKEN
#        OPENROUTER_API_KEY
#        TARGET_CHANNEL_ID
#   2. Настроить keep-alive:
#      https://cron-job.org → every 10 min → GET на URL сервиса
#      (URL появится в Dashboard после деплоя)
# =============================================================================

services:
  - type: web_service
    name: steal-n-scroll-bot
    runtime: docker
    plan: free
    healthCheckPath: /